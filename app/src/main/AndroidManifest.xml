<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.smadacm.reciperepo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RecipeRepo"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">

        <!-- Activities -->
        <activity android:name=".views.list.RecipeList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.list.widget.Item"
            android:label="@string/title_activity_recipe_list_item"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".views.view.ViewRecipe" />
        <activity android:name=".views.view.widget.parts.Item" />
        <activity android:name=".views.view.widget.parts.Header"></activity>
    </application>

</manifest>